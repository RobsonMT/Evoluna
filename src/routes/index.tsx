import { Switch } from "@chakra-ui/react";
import { AnimatePresence } from "framer-motion";

export const Routes = () => (
  <AnimatePresence>
    <Switch>{/* <Routes exact path="/" component={} /> */}</Switch>
  </AnimatePresence>
);
